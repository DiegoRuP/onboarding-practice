export interface MenuItem {
    label: string;    // Texto a mostrar en el menú
    route: string;    // Ruta interna a la que debe navegar
    icon?: string;    // (Opcional) Clase CSS o nombre de icono para mostrar junto al label
  }
  